# ─── Netlify configuration ─────────────────────────────────────────────────
# Deploys apps/web (Next.js 14) from the Turborepo monorepo with npm workspaces.

[build]
  # Run from the repo root so npm workspaces and Turbo can resolve local packages.
  base    = "."
  command = "npm run build -- --filter=@chequealo/web..."
  publish = "apps/web/.next"

[build.environment]
  NODE_VERSION              = "20"
  NPM_VERSION               = "11.13.0"
  TURBO_TELEMETRY_DISABLED  = "1"
  # Turborepo remote cache (optional — set in Netlify UI).
  # TURBO_TOKEN  = ""
  # TURBO_TEAM   = ""

# Next.js runtime adapter — handles SSR, ISR, image optimisation, etc.
[[plugins]]
  package = "@netlify/plugin-nextjs"

# ─── Headers ────────────────────────────────────────────────────────────────
[[headers]]
  for = "/*"

  [headers.values]
    X-Frame-Options        = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy        = "strict-origin-when-cross-origin"
    Permissions-Policy     = "camera=(), microphone=(), geolocation=()"

# ─── Redirects ──────────────────────────────────────────────────────────────
# Redirect bare domain to www (adjust to your domain).
# [[redirects]]
#   from   = "https://chequealo.ai/*"
#   to     = "https://www.chequealo.ai/:splat"
#   status = 301
#   force  = true
